#include "main.h"

/**
 *mul - multiplies two integers
 *@a: first number to multiply with b
 *@b: second number to multiply with a
 *Return: the product of the two numbers a and b
 */

int mul(int a, int b)
{
	return (a * b);
}

